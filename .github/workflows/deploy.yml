name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache raw data files
        uses: actions/cache@v4
        with:
          path: data/raw
          key: raw-data-${{ hashFiles('scripts/download_data.py') }}
          restore-keys: raw-data-

      - name: Install Python dependencies
        run: pip install requests pandas openpyxl rapidfuzz beautifulsoup4 lxml

      - name: Generate city data
        run: |
          if [ -f public/data/cities.json ]; then
            echo "cities.json already present — skipping data download."
          else
            echo "cities.json missing — running data pipeline..."
            python scripts/download_data.py
          fi

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: node node_modules/vite/bin/vite.js build
        env:
          VITE_BASE_URL: /${{ github.event.repository.name }}/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
